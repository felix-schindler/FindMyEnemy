stages:
  - build
  - test

services:
  - postgres:alpine


#frontend_build:
 # stage: build
  #image: node:19-alpine
  #script:
   # - cd frontend
   # - npm install
   # - npm run build

backend_build:
  stage: build
  image: denoland/deno:alpine
  script:
    - cd backend
    - deno task compile

#frontend_test:
 # stage: test
  #image: node:19-alpine
  #script:
   # - cd frontend
    #- npm install
    #- npm run test

backend_test:
  stage: test
  image: denoland/deno:alpine
  script:
    - cd backend
    - deno task test && deno fmt --check && deno lint